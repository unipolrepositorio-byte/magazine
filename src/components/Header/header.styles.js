import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles(theme => ({
    header: {
        position:'absolute',
        zIndex:'10',
        width:'100%',
        display: 'grid',
        height: 'max-Content',
    },
    
}));
export default useStyles;